# 2026-06-16 — Shell GitHub Update Chrome, Usage, and Folder.md Connector

Source: Houston request in Codex, 2026-06-16.

Raw prompt:

> also I see this "houstongolden/houstongolden-you-md" which is my you.md github repo second brain etc it is above the main chat area but I think it should be right aligned so it is next to the github icon - and that github icon should show a little yellow/green/red/purple (use whatever is the exact same color states that github uses for their own status dots PRs commits merged whatever etc! the dot depending on if it is in sync, or behind, or ahead, or disconnected and needs for some reason to be reconnected ... and that github url that is floating left which should float right now should only be visible when you hover on the github icon ... and I kinda wish the github icon was a different style and more solid fill and/or in the cirlce so it stands out a little more please and maybe add a little date last synced or did PR/Merge etc next to it small too ... and the little refresh icon should actually automatically be triggered since we use convex and things are real-time if another local agent or any other agent connected to the username-you-md repo pushes/merged to main it will sync down here in real-time etc i think would be nice ... and you have a publish and a deploy button and i am really unclear on what does what or the difference in those if any difference we should actually just change that to a real small button not an icon that just says [ update ] -- and that will handle the PR/resolve conflicts/merge etc with little streaming steps below it as it is completed actually more detailed steps should just stream into the shell chat session with any details on the commit message and updates that are being pushed - would be nice to see it in a little custom chat stream artifact that shows the structured response steps the agent took in and the full PR message saved for context in the history or something and at least in the chat as a little artifact that I can click to open/expand which will be nice-- and also we probably want a way to save artifacts / markdown files etc in a Files (like a library basically of artifacts and markdown and other rich format file types etc - anyways in the Connectors page we should also add optional "increased free storage up to 10GB when you connect your free folder.md account/api key etc - and we can include a custom prompt in the Folder.md Connector section and link to that section from a separate Settings link for something like Usage - which can include and account for everything from you agent llm calls (this can be reduced when you use the youstack/ystack etc and api/mcp inside of your existing local agents instead of using our CLI/web/apps to chat directly maybe? i dunno just thinking here...) but we don't want to discourage or encourage reducing our agent llm calls we want to encourage AS MUCH usage as possible .... absolutely... anyways...
>
> Usage: You Agent Tokens, API/MCP calls to our own and your custom api/mcp/stack etc, additional things we should have like Crons (Loops), Crawlers, Custom integrations and skills and workflows for your agent, and custom .env if we allow you to BYOK to add other API - other models - Connector apps, etc etc.. i dunno just brain dumping again

Implementation notes:
- Immediate UI: right-align the repo/GitHub state, hide repo name until GitHub hover, replace refresh/publish/deploy icon cluster with one `[ update ]` button, and stream update steps into the shell chat.
- Connector UI: make folder.md a first-class artifact/storage connector with optional 10GB storage language and a copyable agent prompt.
- Usage UI: expose the usage surface across You Agent tokens, API/MCP calls, loops/crons, crawlers, connectors, BYOK/custom env, and artifact storage.
- Follow-up product work: real PR/conflict/merge/update orchestration, expandable update artifacts, saved PR/commit messages in history, Settings -> Usage, and first-class artifact/rich-file persistence.
