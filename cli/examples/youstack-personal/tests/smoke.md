# Smoke Test

Read-only checks:

- Parse `youstack.json`.
- Verify required files exist.
- Confirm declared capabilities are routable.
- Confirm host adapter paths are safe relative paths.
- Do not mutate brain data, connected tools, or repo files.
